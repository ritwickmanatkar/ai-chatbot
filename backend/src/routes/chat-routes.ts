// Imports
import { Router } from 'express';

// NOTE: These are protected functionalities. 
// Only authenticated user can have access to it. 

// User Router Definition
const chatRouter = Router();

// Routes
chatRouter.post('/new', );

// Export chat Router
export default chatRouter;